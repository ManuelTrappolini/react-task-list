export default function AppHeader() {
    const title = 'React Task List';

    return (
        <header>
            <div>
                <h1>
                    {title.toUpperCase()}
                </h1>
            </div>
        </header>
    )
}